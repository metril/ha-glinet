{
  "config": {
    "step": {
      "user": {
        "title": "Connect to GL.iNet router",
        "description": "Enter the router's address and admin password.",
        "data": {
          "host": "Host",
          "password": "Admin password"
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate",
        "description": "Enter the router's admin password again.",
        "data": {
          "password": "Admin password"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the router.",
      "invalid_auth": "Invalid password.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "This router is already configured.",
      "reauth_successful": "Re-authentication was successful."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "GL.iNet options",
        "data": {
          "scan_interval": "Polling interval (seconds)",
          "config_scan_interval": "Config refresh interval (seconds)",
          "enable_device_tracker": "Create device trackers for connected clients"
        },
        "data_description": {
          "scan_interval": "How often to poll dynamic status (connectivity, clients, VPN/repeater state).",
          "config_scan_interval": "How often to re-read rarely-changing config (Wi-Fi, mode, LED, Tor). Edits made from Home Assistant still refresh immediately."
        }
      }
    }
  }
}
